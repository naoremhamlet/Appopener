import React, { useState } from "react";
import styled from "styled-components";

function Renewal() {
  return <RenewalContainer>Renewal</RenewalContainer>;
}
export default Renewal;
const RenewalContainer = styled.div``;
